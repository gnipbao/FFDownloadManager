[package]
name = "ffdownload-manager"
version = "0.3.4"
edition = "2021"
rust-version = "1.95"
description = "A Rust HTTP download MVP with reproducible benchmarks"
publish = false

[workspace]
members = ["src-tauri"]
default-members = ["."]
resolver = "2"

[lib]
name = "ffdownload"
path = "src/lib.rs"

[[bin]]
name = "ffdm"
path = "src/main.rs"

[dependencies]
anyhow = "1"
async-trait = "0.1"
bbdown-core = "=0.5.0"
axum = "0.8"
bytes = "1"
base64 = "0.22"
clap = { version = "4", features = ["derive"] }
fs2 = "0.4"
futures-util = "0.3"
http-body-util = "0.1"
hudsucker = { version = "=0.25.0", default-features = false, features = ["rcgen-ca", "rustls-client", "http2"] }
rcgen = { version = "=0.14.10", default-features = false, features = ["aws_lc_rs", "pem", "x509-parser"] }
regex = "1"
flate2 = "1"
brotli = "8"
time = "0.3"
percent-encoding = "2"
gosh-dl = { version = "=0.6.3", default-features = false, features = ["http", "storage"] }
# Explicitly enable H2: gosh-dl disables reqwest's default features.
reqwest = { version = "0.13", default-features = false, features = ["rustls", "stream", "http2", "system-proxy"] }
reqwest_media = { package = "reqwest", version = "0.12", default-features = false, features = ["native-tls", "system-proxy"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "time", "net", "fs", "process"] }
tokio-tungstenite = { version = "=0.30.0", default-features = false, features = ["connect"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ytdown = { version = "=0.8.0", default-features = false, features = ["native-tls"] }

[dev-dependencies]
boa_engine = "0.20"

[profile.release]
lto = "thin"
codegen-units = 1
